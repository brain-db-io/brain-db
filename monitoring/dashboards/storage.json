{
  "title": "Brain — Storage",
  "uid": "brain-storage",
  "tags": ["brain", "storage"],
  "schemaVersion": 39,
  "version": 1,
  "editable": true,
  "time": { "from": "now-6h", "to": "now" },
  "panels": [
    {
      "id": 1,
      "type": "timeseries",
      "title": "Arena used / capacity (deferred)",
      "description": "Awaiting `brain_arena_used_bytes` / `_capacity_bytes` (deferred: phase-12/storage-stat-api).",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "targets": [
        { "expr": "brain_arena_used_bytes", "legendFormat": "used {{shard}}", "refId": "A" },
        { "expr": "brain_arena_capacity_bytes", "legendFormat": "capacity {{shard}}", "refId": "B" }
      ]
    },
    {
      "id": 2,
      "type": "timeseries",
      "title": "Arena slot usage (deferred)",
      "description": "Awaiting `brain_arena_slots_used` / `_slots_free`.",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "targets": [
        { "expr": "brain_arena_slots_used", "legendFormat": "used {{shard}}", "refId": "A" },
        { "expr": "brain_arena_slots_free", "legendFormat": "free {{shard}}", "refId": "B" }
      ]
    },
    {
      "id": 3,
      "type": "timeseries",
      "title": "WAL size (deferred)",
      "description": "Awaiting `brain_wal_size_bytes` (deferred: phase-12/storage-stat-api).",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "targets": [
        { "expr": "brain_wal_size_bytes", "legendFormat": "{{shard}}", "refId": "A" }
      ]
    },
    {
      "id": 4,
      "type": "timeseries",
      "title": "Metadata size (deferred)",
      "description": "Awaiting `brain_metadata_size_bytes`.",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "targets": [
        { "expr": "brain_metadata_size_bytes", "legendFormat": "{{shard}}", "refId": "A" }
      ]
    }
  ]
}
